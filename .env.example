# CastBridge - Signing Configuration
# Copy this file to .env and set your own values:
#   cp .env.example .env
#
# These values are used to sign the release APK.
# Use any password you want (minimum 6 characters).

KEYSTORE_PASSWORD=changeme
KEY_ALIAS=castbridge
KEY_PASSWORD=changeme
